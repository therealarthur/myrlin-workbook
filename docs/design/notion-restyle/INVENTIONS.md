# INVENTIONS: components and tokens this project derived rather than captured

| Field | Value |
| --- | --- |
| Status | Living. Opened in phase P0 (work package P0.3), appended every phase. |
| Opened | 2026-08-13 |
| Required by | `BUILD-CONTRACT.md` 5.5.5, the deliverables gate. `PROCEDURE.md` section 7. |

The captured Notion bundle at `docs/design/notion-import/_ds/` contains a design system for a
document editor and a marketing site. Myrlin Workbook is neither. Every component and token this
project needs that the capture does not contain is an **invention**, and every invention gets a row
here before it ships.

A row answers four questions:

1. **Nearest relative.** What in the captured system is this closest to?
2. **Inherited.** Which properties were taken from that relative unchanged?
3. **Invented.** What had to be decided from nothing, and on what principle?
4. **Where.** The file and the phase.

An invention is not a licence to design freely. The order of preference is always: use a captured
component, then derive from the nearest captured relative, then invent. Reaching row four without
having tried rows one and two is a review failure.

---

## Tokens

`test/notion-token-parity.test.js` reads this file's token names out of the table below. A token
that `styles.css` defines in a bundle-owned family but that the bundle does not contain must appear
here, or the parity test fails. That is the enforcement: an invention cannot be smuggled in as a
typo.

| Token | Value | Nearest relative | Inherited | Invented | Where | Phase |
| --- | --- | --- | --- | --- | --- | --- |
| `--app-on-accent` | `#ffffff` in both chrome themes | `--mkt-white`, which is what the bundle's own `nt-btn-app` paints its label with | The colour itself, white, and the role, ink on the primary action | That it is a **token** at all, and that it is theme invariant. The bundle reaches for a marketing token here, and `DESIGN-SPEC.md` 16.2 puts `--mkt-*` on the rejection list for the app surface. The obvious app-surface substitute, `--app-text-inverse-primary`, resolves to `#191919` in dark chrome, which is near-black ink on a mid blue and fails the contrast floor. `--app-accent-blue` does not change between themes, so its ink must not either. | `styles.css` `:root` | P1 |
| `--app-scrim` | `rgba(15,15,15,0.55)` | `--app-image-overlay` | The rgba form and the near-black hue | The alpha. The captured overlay is tuned for an image inside a document; Workbook modals sit over a dense application with live terminals behind them, and `DESIGN-SPEC.md` 8.3 says follow the mock and go darker so the modal wins attention. | `styles.css` `:root` | P1 |
| `--app-terminal-gutter` | `var(--app-bg-primary)` | Nothing. The capture contains no terminal. | The value, which is just the canvas | The name. The terminal grid's gutter is given its own token even though it currently resolves to the canvas, so that a later decision to tint it is a one-line change rather than an edit to every grid rule. | `styles.css` `:root` | P1 |
| `--radius-pill` | `999px` | `--radius-round`, `624.9375rem`, which the capture builds `--radius-badge` and `--radius-menu-button` on | The role: one radius large enough that any control height resolves to a true pill, applied to the account chip, the attention badge and the count badges | The value, and the fact that the token exists at all. `--radius-round` cannot be authored: `DEVIATIONS.md` DV-4 records that the capture's own `.mkt-theme-academic` and `.mkt-theme-serif` blocks re-declare every ramp rung including `--radius-round` as `0`, so the value the parity gate reads from the bundle is `0` and copying it would ship square pills. `999px` is what the mock itself writes for the account chip (`DESIGN-SPEC.md` 1.4), and above roughly twice the tallest control it is visually indistinguishable from `624.9375rem`. | `styles.css` `:root` | P2 |
| `--font-terminal` | `"iA Writer Mono", "JetBrains Mono", var(--font-code)` | `--font-code`, the capture's native-metrics code stack, and `--font-mono`, its editor face | The chain's tail: everything after the first two families is `--font-code` verbatim, so `DECISIONS.md` 3.3 and OQ-2's default remain the floor | That the terminal face is a token at all, and that it is a THIRD one. The capture has no terminal, so it has no opinion; the two faces it does have answer different questions, and the terminal face has a constraint neither shares, which is that changing it changes the PTY column count. Splitting it out is what lets `terminalSurface()` project a face per theme without touching the editor's, and what makes the orchestrator's iA Writer Mono default a one-line change rather than an edit inside `terminal.js`. iA Writer Mono is first because it is vendored and self hosted; JetBrains Mono is second because it was the shipped terminal face before P5 and a user who has it installed locally keeps exactly what they had. | `styles.css` `:root`, read by `terminal-surface.js` | P5 |
| `--app-sidebar-row-height` | `32px` | `--app-sidebar-item-height`, the captured `27px` | The role: the minimum height of a top-level row in the left rail, and the fact that it is expressed as one token rather than as a literal in each rule | The value, and the reason there are two names. The captured 27px is a true measurement of Notion's sidebar, and it stays true, so it is not edited. Notion's sidebar rows carry a page name; this rail's rows carry a name, a status mark, a provider identity, a size, a time and up to three tags, and round 1 post-launch the user's verdict on 27px was "really really clustered". A row that holds six things needs more height than a row that holds one. | `styles.css` `:root`, read by `.workspace-item` and `.project-accordion-header` | round 1 |
| `--app-sidebar-subrow-height` | `30px` | `--app-sidebar-row-height` above it | The role, one step of nesting down | The value and the two-step ladder. The nested session rows were 26px against a 27px parent, a one pixel difference that read as noise rather than as hierarchy. 30px against 32px is the same one-step relationship at a density the rows can actually hold. | `styles.css` `:root`, read by `.ws-session-item`, `.project-session-item`, `.sidebar-recent-row` | round 1 |
| `--app-sidebar-section-gap` | `14px` | Nothing captured. The bundle's sidebar separates sections with a label and no measured gap. | The principle from `DESIGN-SPEC.md` 3.5, that separation in this rail is vertical space rather than a rule | The value. Round 1's brief asked for 12 to 16px between sections; 14px is its centre and it is the only number in the rail that says "a new list starts here" without drawing anything. | `styles.css` `:root`, read by `.sidebar-header` | round 1 |
| `--app-status-mix` | `65%` | Nothing captured. The bundle has no status vocabulary at all; a document editor has no session that can be running. | Only the idea of a ratio expressed as a token, which the bundle does for opacity but never for a mix | The value, and MEASURED rather than chosen. It is the proportion of a status hue in the label ink once `DECISIONS.md` 13.6 removed the wash that used to carry the contrast. Below 65 percent the yellow label falls under the 4.5:1 text floor on the white canvas (it reaches 4.56:1 at 65); above roughly 80 percent the hue overwhelms the chrome's own ink and the same label falls under the floor from the other side. One constant for all five hues, because a per-hue ratio is five numbers nobody will keep in step. | `styles.css` `:root`, consumed by the five `--app-status-ink-*` tokens below | 13.6 sweep |
| `--app-status-ink-green` | `color-mix(in srgb, var(--app-text-green) var(--app-status-mix), var(--app-text-primary))` | `--app-chip-green-ink`, the captured ink for a green chip | The role, ink that says "green" | That it is mixed against the chrome's OWN primary ink rather than authored per theme. The captured chip ink is authored to sit on a coloured wash and resolves to a near-white on dark chrome, which carries no hue once the wash is gone; the bare `--app-text-green` is authored as a 7px dot against the 3:1 graphic floor and measures 3.62:1 as text on white. Mixing the hue with `--app-text-primary` is the only form that needs no per-chrome block, because every chrome and every theme already redefines that one token. | `styles.css` `:root`, read by `.status-badge-running` | 13.6 sweep |
| `--app-status-ink-yellow` | `color-mix(in srgb, var(--app-text-yellow) var(--app-status-mix), var(--app-text-primary))` | `--app-chip-yellow-ink` | The role | As above, and this is the hue that SET the ratio: `DEVIATIONS.md` DV-14 measured the bare yellow at 2.68:1 on the light canvas, the worst of the ten status hues, so the mix is tuned until yellow clears the text floor and the other four follow for free. | `styles.css` `:root`, read by `.status-badge-idle` and `.status-badge-needs-input` | 13.6 sweep |
| `--app-status-ink-red` | `color-mix(in srgb, var(--app-text-red) var(--app-status-mix), var(--app-text-primary))` | `--app-chip-red-ink` | The role | As above. Measures 6.58:1 on both grounds at the shared ratio. | `styles.css` `:root`, read by `.status-badge-error` and `.status-badge-failed` | 13.6 sweep |
| `--app-status-ink-teal` | `color-mix(in srgb, var(--app-text-teal) var(--app-status-mix), var(--app-text-primary))` | `--app-chip-teal-ink` | The role | As above. Measures 6.12:1 light and 7.09:1 dark. | `styles.css` `:root`, read by `.status-badge-complete` | 13.6 sweep |
| `--app-status-ink-brown` | `color-mix(in srgb, var(--app-text-brown) var(--app-status-mix), var(--app-text-primary))` | `--app-chip-brown-ink` | The role | As above. Brown is the stale state, kept as its own hue rather than folded into the gray of stopped, because a stale session and a stopped one are different claims and the label alone was carrying that difference once the washes went. | `styles.css` `:root`, read by `.status-badge-stale` | 13.6 sweep |
| `--radius-pane-frame` | `8px` | `--radius-callout` `10px` and `--radius-block-hover` `6px`, the two captured radii it sits between | The idea of a semantic radius name rather than a rung number | The value. `DESIGN-SPEC.md` 1.4 lists 8px as **mock-local**: the pane frame, the cost card, the drop slot, the board column and the mobile cards are all drawn at 8px and the captured token file contains no 8px radius, because a document editor has no pane frame. Rounding these to the 10px callout would erase the mock's own distinction between a frame and a callout, and rounding down to 6px would make a 260px board column look like a button. | `styles.css` `:root` | P2 |

---

## Components

The list to expect, from `DESIGN-SPEC.md`: the terminal pane header, the pane grid gutter, the
session status dot system, the provider identity pill, the attention queue, the cost meter, the
usage meter, the kanban board card, the worktree review banner, the mobile tab bar and the
Select-mode strip. One is recorded so far.

| Component | Nearest relative | Inherited | Invented | Where | Phase |
| --- | --- | --- | --- | --- | --- |
| **The history surface** (`.terminal-history`, the Unified Scrollback Surface) | Two, and neither is close. Inside this application, the Copy view overlay: a `<pre>` of terminal text, parented on the pane, selectable as ordinary DOM. Outside it, a terminal emulator's own scrollback. | From the Copy view: the pane as the positioned host, `white-space: pre-wrap` with `overflow-wrap: anywhere`, native DOM selection as the copy mechanism, and the mirror API as the transcript source. From a terminal: the gesture (wheel up), the keys (`Shift+PageUp`, `Escape`), and the rule that typing returns you to live. From the terminal region itself (P5.4): the ground, the ink, the rule colour and the face, so it is the same surface rather than a panel over it. | That it is NOT a panel. The Copy view announces itself with a bar, a title, a border and a `--mantle` ground, because it is a tool you open. This announces nothing: no border, no shadow, no backdrop, no header, no toggle, and its typography is measured off the live xterm instance at open time rather than authored, so the boundary between it and the terminal is invisible. What had to be invented from nothing is the SEAM: a single hairline in the palette's own rule colour with no label, marking a deliberate one-turn overlap between the transcript and the live frame, chosen over a heuristic join because a wrong join deletes real conversation silently (`TERMINAL-ARCHITECTURE.md` 7.4). The document also had to be given an order that serves two different data sources with one layout: deep, ring, transcript, live, oldest first, with the CURRENT SCREEN as the last segment, which is what makes a drag from history into the live screen one contiguous selection instead of a boundary case. | `terminal-history.js`, `styles.css` | P7 |
